prototype(Vendor.Site:Document.Page) < prototype(Neos.Fusion:Component) {
  renderer = Vendor.Site:Document.Base {
    body = Vendor.Site:Layout.Page {
      header = Vendor.Site:Block.SiteHeader

      main = Neos.Neos:ContentCollection {
        nodePath = 'main'
      }

      footer = Vendor.Site:Block.SiteFooter
    }
  }
}
